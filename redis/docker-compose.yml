version: '2'
services:
  redis:
    image: redis:6.0
    restart: always
    container_name: redis
    command: redis-server --requirepass root
    ports:
      - "6379:6379"
    volumes:
      - /data/redis:/data
    privileged: true